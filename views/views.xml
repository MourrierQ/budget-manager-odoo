<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="budget_manager.list">
      <field name="name">budget_manager list</field>
      <field name="model">budget_manager.budget_manager</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="budget_manager.action_window">
      <field name="name">budget_manager window</field>
      <field name="res_model">budget_manager.budget_manager</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="budget_manager.action_server">
      <field name="name">budget_manager server</field>
      <field name="model_id" ref="model_budget_manager_budget_manager"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="budget_manager" id="budget_manager.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="budget_manager.menu_1" parent="budget_manager.menu_root"/>
    <menuitem name="Menu 2" id="budget_manager.menu_2" parent="budget_manager.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="budget_manager.menu_1_list" parent="budget_manager.menu_1"
              action="budget_manager.action_window"/>
    <menuitem name="Server to list" id="budget_manager" parent="budget_manager.menu_2"
              action="budget_manager.action_server"/>
    -->
  </data>
</odoo>